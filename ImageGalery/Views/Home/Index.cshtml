@using PagedList.Mvc
@model PagedList.IPagedList<ImageGalery.Models.ImageModel>

@{
    ViewBag.Title = "Image Galery";
}

@foreach (var i in Model)
{
    Html.RenderPartial("_ImagePartial", i);
}

@Html.PagedListPager(Model, page => Url.Action("Index", new {page}))